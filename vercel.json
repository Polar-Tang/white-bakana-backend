{
  "version": 2,
  "builds": [
    {
      "src": "/(.*)",
      "use": "@vercel/node" 
    }
  ],
    "routes": [
      {
        "src": "/(.*)", 
        "dest": "/(.*)",
         "methods": ["GET", "POST", "OPTIONS"],
         "headers": {
          "Access-Control-Allow-Origin": "*",
         "Access-Control-Allow-Methods": "GET, POST, OPTIONS"
         }
        }
  ]
   
  
  
}
